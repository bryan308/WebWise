import Link from 'next/link';
import { Card, MDXImage, MDXLink, Note, PImage, Section } from './elements';

export const components = {
	Section,
	MDXLink,
	Card,
	MDXImage,
	Note,
	PImage,
	Link,
};
